# SDD ledger — plan: /private/tmp/forge-planner-adversarial-resynced/docs/superpowers/plans/2026-08-01-adversarial-review-remediation.md

- Branch start: 78f496a1ee442a062dd667b2fb85825fbd468261
- Execution workspace: /private/tmp/forge-planner-adversarial-resynced
- Process decision: one isolated plan worktree with task-scoped commits and serialized integration, preserving the plan's overlap constraints while following the SDD single-workspace review loop.
- Pre-flight conflict scan: clean; no plan item contradicts the global constraints or mandates a known review defect.
- Plan artifacts preserved in commit fc4caf9.
- Task 0: fix round 1/5 (3 addressed, 0 open; commits 1b56e34..47db0b4)
- Task 0: complete (commits fc4caf9..47db0b4, review clean)
- Task 1: minor (deferred): recovery dismissal restores Import focus rather than the exact Export/Import invoker.
- Task 1: review warning resolved: imported-markup sinks and direct mutation ownership are explicitly assigned to Tasks 2 and 3; untouched mechanics remain covered by the existing suite and final audit.
- Task 1: fix round 1/5 (4 addressed, 0 open; commits b6b1f30..5cbbcea)
- Task 1: complete (commits 47db0b4..5cbbcea, review clean)
- Task 2: fix round 1/5 (2 addressed, 0 open; commits faf813d..3c83d46; local SDD report untracked)
- Task 2: complete (commits 5cbbcea..3c83d46, review clean)
- Task 3: minor (deferred): an onerror from an owned but idle reused Worker can increment failure state and expose fallback with no authoritative request.
- Task 3: review warning resolved: Tasks 4–7 and Task 13 are explicitly downstream; full-suite evidence is recorded in the implementer report.
- Task 3: fix round 1/5 (2 addressed, 0 open; commits 8d1e819..c4e5c39)
- Task 3: complete (commits 3c83d46..c4e5c39, review clean)
- Execution order adjustment: prioritize Tasks 9, 10, and 11A next to deliver approved P1 UI/UX geometry sooner; all declared dependencies are satisfied, and Tasks 4–8 remain pending.
- Task 9: minor (deferred to Task 11C): dialog inert cleanup does not preserve pre-existing inert state on independent body children.
- Task 9: review warning resolved: full Playwright and in-app QA evidence is recorded in the implementer report; the reviewer correctly did not rerun broad gates.
- Task 9: fix round 1/5 (3 addressed, 0 open; commits 8063680..7e11d73)
- Task 9: complete (commits c4e5c39..7e11d73, review clean)
- Task 10: minor (deferred): skip-link destination suppresses a visible focus indicator after activation.
- Task 10: review warning pending manual verification: real screen-reader timing and rendered 200% zoom/forced-colors/keyboard/mobile states will be included in Task 10 fix evidence and Task 11A visual QA.
- Task 10: fix round 1/5 (5 addressed, 1 open; commits d9fb98f..b4264fb)
- Task 10: fix round 2/5 (1 addressed, 0 open; commits b4264fb..eb950d4)
- Task 10: complete (commits 7e11d73..eb950d4, review clean; manual announcement timing remains final rendered QA)
- Task 11A: visual review approved all 13 before/after pairs; P2 observations deferred to 11B–11D.
- Task 11A: minor (deferred): CI visual step is unnamed and test:browser re-runs visual-layout because only accessibility is excluded.
- Task 11A: review warning resolved: RED/GREEN chronology, command evidence, flaky isolation, and live 561px inspection are recorded in the implementer report and ignored visual artifacts.
- Task 11A: complete (commits e2879ca..f1e9bee; all 13 visual pairs approved; checkpoint merged to main by PR #93 at 5c351ca).
- Emergency main audit/resync: branch `codex/adversarial-remediation-continuation-v2` starts clean at `1466a5d`, including `188e913` (permanent legacy Worker fence plus v2 source endpoint) and `1466a5d` (content-hashed static build, embedded Blob Worker, frozen compatibility endpoint, production cache headers/tests).
- Plan reconciliation: commit `8940423` adds the global release/Worker contract and Task 3F; redirects current Worker work to `js/solver.worker.v2.js` plus the build graph; protects both permanent compatibility endpoints; constrains Tasks 5–8 and Task 13 to current factory/Blob seams; extends Task 11D for hashed/base-aware fonts; rewrites Task 14 around remaining subpath/warm-upgrade gaps; preserves every deferred minor owner; narrows Task 15 and expands Task 16 verification. Re-review also assigns inert-state restoration to Task 11C, adds the frozen v2 request fixture plus its existing browser-smoke owner to Task 3F, gives Task 4 browser-smoke ownership, and includes `js/render.js` in Task 14 tooltip-path scope.
- Task 3F: fix round 1/5 (2 addressed, 0 open; commits `4741fbb..5c06a4f`): post-construction factory setup exceptions now terminate/release the created Worker, and idle errors silently discard the unhealthy Worker before a healthy replacement solve. Fresh scoped re-review approved with no findings.
- Task 3F: complete (commits `8940423..5c06a4f`, review clean; controller independently reran 11/11 lifecycle, 6/6 static-build, 3/3 compatibility, browser-spec syntax, all 19 Node scripts with parity 16/16, deterministic build, compatibility-byte diff, and `git diff --check`; no browser launched).
- Task 4: implementation committed as `1e36613` from base `5c06a4f`; independent review in progress. Direct/full gates are green with 20 Node scripts and parity 16/16; no browser launched. Read-only design preparation confirmed the exact `-2932.417170` Ingot transient, resolved pre-produced Frames/Wire Bits as explicit external prerequisites rather than Project LP lines, separated semantic `phases` from executable `executionPhases`, and replaced approximate clamped carry with exact replay ownership.
- Task 4: fix round 1/5 in progress after independent review found 5 Important blockers: malformed schedule shapes fail open or throw; callback phases can inject external/untracked/unrelated resources; combined-root dependency recursion rejects a legitimate sibling-root ancestor; partial-stock Bits copy states the shortfall as the total actionable prerequisite; and blocked waved results retain imperative ordering copy. Reviewer Minor coverage/cleanup observations are included in the same fix round where safe. No browser is authorized locally.
- Task 4: fix round 1/5 complete (commit `5e619fe`, 5 Important findings and all cheap reviewer Minors addressed, fresh scoped rereview approved with no Critical/Important/Minor findings). Controller independently reran all 20 Node scripts with parity 16/16, deterministic build, Project/stability/static/legacy focused gates, browser-spec syntax, compatibility-byte diff/checksums, and `git diff --check`; no browser launched. Task 4 is complete across commits `5c06a4f..5e619fe`.
- Task 5 preparation: controller accepted the explicit 64-line complexity boundary in `task-5-brief.md`. `gelLoadout` remains exact/uncapped and is used by the capacity UI through 12 lines; solver seeds and >12-line UI use a separately named bounded helper, with the large-factory UI labeled `Estimated capacity` / `Best found loadout`. The 64-line save limit remains compatible, and no bounded result may claim an exact maximum.
- Task 5: fix round 1/5 resolved assignment-order-dependent strict-budget feasibility and cross-signature dominance pruning (`e5affec..707772e`). Stable low-to-high aggregation and remaining-profile rank signatures now preserve exact physical-line equivalence.
- Task 5: fix round 2/5 widened the dominance envelope to cover all four stable-sum error paths and protected far-cost Gel ordering (`707772e..a7dc5e5`).
- Task 5: fix round 3/5 protected ordinary dominance from stable-sum Vespium-order reversal with an independent order-safe monotone query (`a7dc5e5..e5418e4`). Fresh scoped review approved the exact safety boundary, both cursor predicates, unchanged public tie semantics, and a 50,000-case optimized-versus-reference comparison.
- Task 5: complete (commits `5e619fe..e5418e4`, review clean). Controller independently reran exact/oracle coverage, the full scale matrix, static release graph, parity `16/0/0`, immutable endpoint hashes, and diff/worktree checks. All 21 Node scripts and browser-spec syntax also passed; no browser was launched. Exact 12-line mixed cases remained below one second, and >12-line UI claims remain explicitly estimated.
- Task 6: implementation committed as `32b70c4`; formal review found two Important blockers: persistent shell labels still promised an optimal setup during bounded Credits results, and stale executable rows beyond the current physical line count could pass Copy-to-Manual eligibility before being truncated to all Idle.
- Task 6: fix round 1/5 complete (commit `49a9c36`, 2 Important findings addressed). Persistent labels are neutral, rendering/copy derive only from current physical lines, missing rows map safely to Idle, and out-of-range rows cannot trigger a mode switch. Fresh independent review approved exact range `32b70c4..49a9c36` with no findings.
- Task 6: complete (commits `e5418e4..49a9c36`, review clean). Controller independently reran the focused Credits contract, all 22 Node scripts, deterministic static build, parity `16/0/0`, browser-spec syntax, immutable endpoint byte/hash checks, and `git diff --check`; no browser was launched.
- Task 7 reconciliation: the executable-schedule model materially refines COR-06. In the frozen 420-Frames case, held phase throughput is 2.6304% lower and phase ETA 2.7015% longer, but held warm-up is 129.94 seconds shorter and the complete held run (`0.6659750249h`) beats reoptimization (`0.6846583163h`) by 67.26 seconds. The plan now uses `prefer-current | reoptimize`, compares complete replayed schedules, isolates hidden comparison/cache effects, and forbids any unconditional `fastest` action or claim.
- Task 7 plan review: fix round 1/5 opened with 5 Important blockers and 2 Minors: literal nonnegative-boundary acceptance contradicted replay tolerance; duplicate accepted project IDs could collide; Worker `__stab` snapshot semantics contradicted unrelated-cache preservation; the selector lacked an accessible name; comparability/percentage/tolerance/nonexecutable action branches were underdefined; the stylesheet path was singular; and Keep-current was a no-op action.
- Task 7 plan review: fix round 2/5 opened with 2 Important blockers and 1 Minor after the first fresh rereview: executable/comparable/cache-success omitted full-demand LP feasibility, multi-phase higher-throughput copy lacked an all-phase quantifier/mixed-gap branch, and the generic signed-field sentence contradicted the named ETA-penalty direction.
- Task 7: implementation committed as `7eea579`; all focused contracts, 23 Node scripts, deterministic/static build, parity `16/0/0`, changed browser-spec syntax, immutable endpoint hashes, and diff checks passed without launching a browser. Formal review found one Important prototype-key crash in the hidden comparison index, one missing explicit import/persist/reload/reset acceptance proof, and a Minor report typo.
- Task 7: fix round 1/5 complete: the normal-object comparison index is now an exact-key `Map`; real 200-to-420 held regressions cover schema-valid `constructor` and `toString` Project IDs; import/persist/reload/reset policy acceptance is explicit; and report evidence is corrected. Focused tests, all 23 Node scripts, deterministic/static build, parity `16/0/0`, changed syntax, immutable endpoint hashes, and diff checks are green without a browser launch. Fresh scoped rereview approved the exact fix range with no Critical, Important, or Minor findings; Task 7 is review-clean.
- Task 7: complete (commits `3962aa3..877c6aa`, fresh scoped rereview approved with no residual findings). Controller independently reran all 23 Node scripts, the focused stability/UI/schema/transient contracts, deterministic static build `8/8`, parity `16/0/0`, compatibility hashes, and `git diff --check`; the worktree is clean and no browser was launched.
- Task 8 preflight: current schema v2 and Task 1 explicitly preserve solve budgets through 60,000ms, so the GUI will expand to the established limit instead of quarantining/coercing valid saves. Numeric/game-notation input will parse before mutation; invalid/incomplete drafts remain DOM-only and cannot reach state, storage, export, rendering, or the Worker. The reconciled file/test scope now includes DOM helpers, solver/results bounds, visual/accessibility coverage, and generated Blob Worker acceptance/rejection while all permanent compatibility artifacts remain frozen.
- Task 8 plan review round 1/5: not approved. No Critical findings; the reviewer caught the dependency edit on Task 2 instead of Task 8, undefined inline-Project feedback ownership, and non-atomic cross-field correction semantics. Minor clarifications cover trailing decimals, a narrow-screen-friendly lossless 60-second budget mapping, and live regions outside nested labels. The fix defines atomic pair commits in either edit order, distinct Shopping-list/inline geometry, pre-existing sibling live regions, and exact preservation of nonstandard valid budgets.
- Task 8 implementation: unified numeric descriptors/parser/formatter, pre-mutation GUI commits, atomic Project endpoint pairs, polite nearby feedback, lossless 200..60000ms Settings stops, descriptor-backed page/solver clamps, and current generated Blob Worker coverage are implemented from emergency-resynced base `299b5f3`. Initial focused RED exposed the missing API/contracts; later RED cycles closed persisted-draft length, mined attribute binding, and truthful tiny-number feedback.
- Task 8 review round 1/5: 2 Important and 1 Minor findings addressed. The accepted Worker smoke fixture now includes required `baseTimeRev`; CI-only coverage now explicitly exercises line turbo, max turbo, margin, target priority, invalid/blank Project quantity, Project priority blank, paste, and correction/blank-resume for Forgie/mined/inventory; exact prior-value formatting is round-trip-safe. The mined renderer harness dependency regression is fixed.
- Task 8 gates after review fixes: focused field `12/12`, state `46/46`, both mined renderer suites, all 24 Node scripts, deterministic build, static release `9/9`, parity `16 ok, 0 improved, 0 failed`, changed runtime/browser syntax, exact accepted Worker-state reproduction, frozen endpoint/fixture/golden hashes, and diff hygiene pass. No browser was launched; browser/Axe/geometry/Blob execution remains CI-only.
- Task 8: complete pending controller gate. Fresh independent re-review approved the complete implementation with no residual actionable findings after confirming the Worker fixture, required browser matrix, exact formatter, mined harness, full Node/static/parity gates, frozen hashes, and diff hygiene. No browser was launched.
- Task 8 formal review of `299b5f3..dcf471d`: 2 Important findings, 0 Critical, 0 Minor. Case-folded/punctuation-collapsing Project feedback tokens could redirect the second row to the first row's live region, and exact `2345ms` state was displayed/announced as rounded `2.3 s`.
- Task 8 formal-review fix round 1/5 implemented from clean `dcf471d`: feedback tokenization is injective over JavaScript strings using selector-safe fixed-width UTF-16 hex; pure duration copy renders validated integer milliseconds as exact trimmed seconds; CI-only Shopping-list and inline Project regressions prove unique IDs, owned feedback, and `aria-describedby` for `ProjectA`/`projecta`; Settings asserts visible and announced `2.345 s` without storage mutation.
- Task 8 formal-review fix round 1 gates: focused RED reproduced both defects, then field `14/14`, state `46/46`, all 24 Node scripts, deterministic build, static release `9/9`, parity `16 ok, 0 improved, 0 failed`, changed syntax, `git diff --check`, and frozen endpoint hashes passed. No browser or Playwright process was launched; fresh formal re-review remains the controller's next gate.
- Task 8 formal re-review approved exact fix range `dcf471d..d809025` with no Critical, Important, or Minor findings. The reviewer independently verified injective selector-safe feedback IDs and ownership in both Project surfaces, exact visible and announced millisecond copy, focused/state tests, changed syntax, diff hygiene, and all four frozen hashes without launching a browser.
- Task 8: complete (plan/implementation commits `4ee7efd..d809025`, review clean). The controller independently reran all 24 Node scripts, deterministic build, static release `9/9`, parity `16/0/0`, frozen endpoint/fixture/golden hashes, syntax, worktree, and diff checks. Browser/Axe/geometry/Blob execution remains CI-only; no local browser was launched.
- Owner scope correction: the merged Task 11A checkpoint is the final UI deliverable for this pass. Tasks 11B–11D and Task 12 are retired from the continuation unless explicitly requested later. Their four deferred code-level minors move to Tasks 14/16; no post-checkpoint visual redesign is authorized.
- Task 13 reconciliation: the original target/price/margin RED is stale—Task 8 saves numeric drafts immediately, and bubbling target `change` synchronously flushes the solve/save path. The real loss is persisted Project `_open` mutating without any save. Task 13 now adds an independent 100ms persistence owner; exact hidden/pagehide behavior; a solve-equivalence-keyed Project cache that ignores only `planStart`/`_open`; the same solve-relevant currentness boundary inside `solveService` so display-only revision bumps cannot discard valid in-flight work; asynchronous Progress refresh through the one authoritative request; and the missing injectable current-Worker factory while preserving the static builder seam and explicit Resimulate behavior.
- Tasks 15–16 reconciliation: preserve already-correct runtime trust copy and fix only the remaining README/index claims; make Task 15 depend on settled Task 13 and add truthful catalog provenance/semantic validation without invented game values. Task 16 now explicitly owns the three deferred code minors (exact recovery invoker, pre-existing inert restoration, and visible skip-target focus), reruns named existing coverage instead of duplicating it, and completes the full mode×viewport matrix with checkpoint-regression-only repairs.
- Task 14 reconciliation: make the current integrated Blob-Worker/release graph an explicit dependency; add the omitted server/config/CI owners; mount one `dist` tree safely at root and subpath with real validators; use incompatible A/B sentinels to prove warm-cache integrity; preserve Analytics absence; and assign each browser lane exactly once in CI.
- PR #94 integration gate: GitHub reports PR #94 mergeable into current `main` (`1466a5d`), but an actual merge-tree against remediation HEAD `83e2b1f` found conflicts in `index.html`, `js/core.js`, `js/events.js`, `js/fields.js`, `js/results.js`, `js/solver.js`, and `js/state.js`. Task 13P now owns the semantic port before Task 14: preserve Set & forget while retaining executable schedules, exact Gel/Credits/stability/validation/persistence contracts and the emergency Worker/release graph.
- Task 13 implementation: independent 100ms persistence ownership, exact page lifecycle ordering, solve-equivalent currentness that ignores only `planStart`/Project `_open`, keyed asynchronous Project Progress, and the current-Worker factory hook are implemented from runtime base `2009776` on top of non-overlapping plan-only head `52c0627`. RED reproduced 4 lifecycle defects, then self-review reproduced and fixed stale key cleanup plus the initial-`undefined` render timer sentinel. Focused lifecycle `21/21`, field `14/14`, static release `9/9`, all 24 Node scripts, deterministic build, parity `16/0/0`, syntax/diff, generated compatibility-byte comparisons, and all four frozen hashes pass. Browser teardown/reload, visibility, slow-factory Progress, and explicit-Resimulate flows are authored for CI only; no browser was launched. No known Task 13 blocker remains; independent exact-diff review is pending controller dispatch.
- Task 13P post-integration adversarial repair: deterministic REDs reproduced deadline fixed-point incumbent loss, a 4×↔2× pre-produced-Bits convergence cycle, false held-Bits disclosure, missing bounded-success disclosure, and incomplete static/order guidance. Static incumbents are now exact-replay-certified before retention; cycle recovery tries strict descending lower ceilings under the same control and compares replay-certified feasible ETA; fallback interruption preserves the certified unrestricted incumbent; unrestricted ceiling metadata stays null; default split solves skip certification and benchmark at baseline. P2 copy is derived from replay execution data and preserves checkpoint geometry. Final independent re-review approved with no findings. Focused static `36/36`, seqgate `37/37`, transients, parity `16/0/0`, full `npm test`, static release `9/9`, deterministic digest `04c9fce882e85b08776ed447772b49268e26f5c2e6298a83f964d1ff7ace6386`, frozen hashes, syntax, and diff checks pass; no browser was launched.
- Task 14: document-relative hashed URLs, dual-mount release serving, contained path resolution, strong validators, same-origin incompatible A→B upgrade coverage, non-overlapping CI lanes, and GUI preview/release commands are complete. Isolated independent review passed static `11/11`, release smoke `10/10`, permanent Worker `3/3`, syntax/diff, and lane discovery with no findings. Browser execution remains CI-only; no browser or persistent preview was launched.
- Task 15: truthful README/footer/Shopping-list claims, schema/solver/catalog/release operator contracts, explicit unknown/unverified catalog provenance, and a registered semantic catalog gate are complete. Independent review reproduced and closed sparse-array, chained-null custom-prototype, and unsafe prerequisite-adjacency gaps; final rereview found no Critical, Important, or Minor issues. Catalog `11/11`, focused trust copy, all 27 Node scripts, parity `16/16`, syntax, and diff checks pass. No browser was launched.
- Task 16 local candidate audit: exact recovery-invoker, nested inert restoration, and visible skip-target focus repairs are complete and independently review-clean. The exact 13-viewport matrix covers four modes, six registered dialogs, persistent controls, result geometry, overflow/scrollers, Project identity, dialog reachability, and sparse recipe cards with four required CI artifacts. Matrix review closed one Important scope gap and two Minor race/evidence gaps; rereview is clean. All 27 Node scripts, parity `16/16`, static `11/11`, compatibility `3/3`, release smoke `10/10`, syntax/YAML/diff, and lane discovery (`85/11/26/4`) pass. Browser execution, rendered preview approval, and a current live-save smoke remain PR/owner gates; no local browser was launched.
